plugins {
    id 'com.android.application'
}

android {
    // Ye line sabse zaroori hai (Github ke liye)
    namespace 'com.example.porterhook'
    
    compileSdk 33

    defaultConfig {
        applicationId "com.example.porterhook"
        minSdk 24
        targetSdk 33
        versionCode 1
        versionName "1.0"
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    // Xposed API
    compileOnly 'de.robv.android.xposed:api:82'
}
